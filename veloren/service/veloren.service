[Unit]
Description=Veloren Dedicated Server

[Service]
User=cristian
WorkingDirectory=/opt/veloren
ExecStart=/opt/veloren/manage.sh --start
ExecStop=/opt/veloren/manage.sh --stop
NonBlocking=true

Restart=on-failure
RestartSec=5
StartLimitBurst=1

[Install]
WantedBy=multi-user.target
